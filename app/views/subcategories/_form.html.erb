<%= form_with(model: subcategory, local: true) do |form| %>
  <% if subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br/>
  <hr/>
  <br/>

  <div class="field">
    <%= form.label :category_ids, 'Categories' %>
    <%= form.select :category_ids, Category.all.pluck(:name, :id), {}, {multiple: true, class: "selectize"} %>
  </div>

  <br/>
  <hr/>
  <br/>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


