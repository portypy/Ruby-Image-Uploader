

<h4> Users list</h4>

<% @users.each do |user|  %>
<ul>
  <li>
    <%= user.email %>
    <%= link_to 'Show', user %>
    <% if user.is_admin?  %>
       is admin!!!!
    <% end %>
    <% user.roles.each do |role| %>
      <%= role.name %>
  <% end %>
  </li>
</ul>

<% end %>

<%= link_to 'Back', posts_path %>
