<h2> users list</h2>

<% @users.each do |user|  %>
<ul>
  <li>
    <%= user.email %>
    <%= link_to 'Show', user %>
    <%#= link_to 'MakeAdmin', user.make_admin %>
    <% user.roles.each do |role| %>
      <%= role.name %>
    <% end %>
  </li>
</ul>

<% end %>

<%= link_to 'Back', posts_path %>
